
while true
      do
      pid=$(ps axu | grep $searching | grep -v grep | awk '{print $2}')
      if  [ -z "$pid" ]; then 
          echo ssh must be restarted .....
          eval $command 
          echo ssh restarted .....
      else
          echo ssh is still running ....

          # Die folgenden Befehle werden über TOR versendet ....

          proxychains ssh -p 22 -A42C redirect01@your-own-ddns-name02.ddnsfree.com ping -c 3 www.google.com > /dev/null
          proxychains ssh -p 22 -A42C redirect01@your-own-ddns-name04.ddnsfree.com ping -c 3 www.google.com > /dev/null
          proxychains ssh -p 22 -A42C redirect01@your-own-ddns-name05.ddnsfree.com ping -c 3 www.google.com > /dev/null
      fi 
      sleep 20 
done
